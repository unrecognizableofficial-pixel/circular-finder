import { CircularIdService } from "@/modules/circular-id/circular-id.service";

describe("CircularIdService", () => {
  it("should be defined", () => {
    expect(CircularIdService).toBeDefined();
  });
});
